[project]
name = "dendamp"
version = "0.0.1"
description = "Controlled-displacement reliability instrument for dendritic network SDMs"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
